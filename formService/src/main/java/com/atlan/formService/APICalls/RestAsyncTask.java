package com.atlan.formService.APICalls;

import okhttp3.OkHttpClient;

public class RestAsyncTask {

    public static OkHttpClient sendError() {
        OkHttpClient client = new OkHttpClient();
        return client;
    }
}
